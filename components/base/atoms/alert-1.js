import * as React from 'react';
import Stack from '@mui/material/Stack';
import Alert from '@mui/material/Alert';

export default function Alert1() {
  
    return (
      <Stack spacing={2} direction="row">
        <Alert variant="text">Text</Alert>
     
      </Stack>
    );
  }